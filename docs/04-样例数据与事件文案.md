# 样例数据与事件文案

## 文档目的

本文件集中整理 V0.1.1 低保真原型需要的静态样例数据和事件文案。设计师、前端或其他智能体可以直接使用本文件填充页面。

## 样例目标

> 修复登录页错误提示不显示，并补充测试。

## 样例计划 6 步

1. 阅读登录页相关代码，定位错误提示组件。
2. 确认错误提示未显示的根因。
3. 修复错误提示显示逻辑。
4. 在登录页测试文件中补充错误提示相关用例。
5. 运行单元测试与组件测试。
6. 生成改动总结与审查建议。

## 样例验收标准 3 条

1. 输入错误密码后，登录页能展示对应错误提示。
2. 新增至少 2 条针对错误提示的测试用例，且全部通过。
3. 不引入新的代码检查或类型检查告警。

## 样例任务卡

### 执行中任务卡

- 任务编号：任务-001。
- 任务名称：修复登录页错误提示不显示。
- 主任务状态：执行中。
- 异常标签：无。
- 负责智能体：工作智能体。
- 已运行时长：4 分 12 秒。
- 验证结果：进行中。
- 风险等级：低。

### 待审查任务卡

- 任务编号：任务-001。
- 任务名称：修复登录页错误提示不显示。
- 主任务状态：待审查。
- 异常标签：无。
- 负责智能体：工作智能体。
- 最近更新：测试全部通过，等待你审查。
- 验证结果：已通过。
- 风险等级：低。
- 主操作：`查看审查包`。

### 已完成任务卡

- 任务编号：任务-001。
- 任务名称：修复登录页错误提示不显示。
- 主任务状态：已完成。
- 异常标签：无。
- 最近更新：任务-001 已接受。
- 验证结果：已通过。
- 风险等级：低。

## 样例运行时间线

| 时间 | 事件 |
|---|---|
| 14:00 | 任务-001 已创建 |
| 14:00 | 独立工作区已创建（branch: fix/login-error-tip） |
| 14:01 | 上下文已生成 |
| 14:01 | 工作智能体已开始 |
| 14:02 | 工作智能体读取了 src/login/Login.tsx |
| 14:03 | 工作智能体修改了 src/login/Login.tsx |
| 14:03 | 工作智能体修改了 src/login/Login.module.css |
| 14:04 | 工作智能体新增了 src/login/Login.test.tsx |
| 14:05 | 运行测试中 |
| 14:06 | 测试全部通过 |
| 14:06 | 审查包已生成 |
| 14:06 | 任务-001 等待审查 |

## 样例上下文

- 角色：工作智能体。
- 任务：修复登录页错误提示不显示，并补充测试。
- 验收标准：见本文件“样例验收标准 3 条”。
- 项目规则：使用项目内通用的错误提示组件，不引入新依赖。
- 工作区：fix/login-error-tip（独立分支）。
- 需要运行的验证：单元测试、组件测试、代码检查、类型检查。
- 上一次反馈：无。
- 安全模式：严格。

## 样例改动

### 修改文件汇总

- 共 3 个文件被修改。
- 总计：+44 / -3。
- 风险等级：低。

### 修改文件列表

| 文件 | 类型 | 变更 |
|---|---|---|
| src/login/Login.tsx | 修改 | +12 / -3 |
| src/login/Login.module.css | 修改 | +4 / -0 |
| src/login/Login.test.tsx | 新增 | +28 / -0 |

### 改动摘要

- 修复登录失败时错误提示未传入错误提示组件的问题。
- 补充错误提示显示区域。
- 新增登录失败场景测试，覆盖密码错误与账号不存在两种场景。

## 样例静态 diff

默认选中 `src/login/Login.tsx`。

```diff
// src/login/Login.tsx
- setErrorMessage("")
+ setErrorMessage(result.errorMessage)

+ {errorMessage && (
+   <ErrorMessage>{errorMessage}</ErrorMessage>
+ )}
```

第二个静态样例可用于点击 `src/login/Login.test.tsx` 后展示。

```diff
// src/login/Login.test.tsx
+ it("登录失败时展示错误提示", async () => {
+   await submitLoginForm({ username: "demo", password: "wrong" })
+   expect(screen.getByText("账号或密码不正确")).toBeVisible()
+ })
```

## 样例验证结果

| 验证项 | 结果 | 说明 |
|---|---|---|
| 单元测试 | 通过 | 28 / 28 |
| 组件测试 | 通过 | 登录失败提示正常显示 |
| 代码检查 | 通过 | 0 警告 |
| 类型检查 | 通过 | 0 错误 |

执行详情页验证区展示：

- 测试：通过（28 / 28）。
- 代码检查：通过（0 警告）。
- 类型检查：通过（0 错误）。

## 样例审查包

- 任务回顾：修复登录页错误提示不显示，并补充测试。
- 智能体总结：登录失败时，错误状态被覆盖导致提示不渲染。已在登录提交逻辑里保留 errorMessage 状态，并在 Login.tsx 中渲染该提示；新增 2 条测试覆盖密码错误与账号不存在两种场景。
- 验收标准结果：3 / 3 通过。
- 修改文件清单：src/login/Login.tsx、src/login/Login.module.css、src/login/Login.test.tsx。
- 同页差异预览：默认展示 src/login/Login.tsx 的静态 diff。
- 验证结果：测试、代码检查、类型检查全部通过。
- 风险提示：本次改动涉及登录路径，建议人工再快速检查一次错误提示的视觉表现。
- 审查建议：改动范围合理，测试覆盖充分，可接受。
- 风险等级：低。

## 样例事件流

倒序展示：

- 14:09 已保存工作流：前端问题修复。
- 14:08 任务-001 已接受。
- 14:08 任务-001 已完成，未保存为工作流。
- 14:07 用户点击接受结果。
- 14:06 任务-001 等待审查。
- 14:06 审查包已生成。
- 14:06 测试全部通过。
- 14:05 运行测试中。
- 14:04 工作智能体新增了 src/login/Login.test.tsx。
- 14:03 工作智能体修改了 src/login/Login.module.css。
- 14:03 工作智能体修改了 src/login/Login.tsx。
- 14:01 工作智能体已开始。
- 14:01 上下文已生成。
- 14:00 独立工作区已创建（branch: fix/login-error-tip）。
- 14:00 任务-001 已创建。

主流程结束时必须出现：

> 任务-001 已接受。

## 事件文案模板

| 事件 | 文案模板 |
|---|---|
| 任务创建 | 任务-XXX 已创建 |
| 计划生成 | 任务计划已生成 |
| 计划确认 | 任务-XXX 已确认并开始执行 |
| 工作区创建 | 独立工作区已创建（branch: XXX） |
| 上下文生成 | 上下文已生成 |
| 执行开始 | 工作智能体已开始 |
| 文件读取 | 工作智能体读取了 XXX |
| 文件修改 | 工作智能体修改了 XXX |
| 文件新增 | 工作智能体新增了 XXX |
| 测试运行 | 运行测试中 |
| 测试通过 | 测试全部通过 |
| 验证未通过 | 任务-XXX 验证未通过 |
| 审查包生成 | 审查包已生成 |
| 待审查 | 任务-XXX 等待审查 |
| 接受结果 | 任务-XXX 已接受 |
| 拒绝并反馈 | 任务-XXX 已被拒绝，反馈已进入下一次运行 |
| 重新执行 | 任务-XXX 已重新执行 |
| 审查智能体复查 | 审查智能体已开始复查 |
| 停止 | 任务-XXX 当前运行已停止 |
| 暂不保存工作流 | 任务-XXX 已完成，未保存为工作流 |
| 保存工作流 | 已保存工作流：前端问题修复 |

## 右侧状态栏样例

默认严格安全模式：

- 安全模式：严格。
- 自动合并：关闭。
- 外部网络：受限。
- 密钥访问：禁止。

智能体状态：

- 计划智能体：空闲。
- 工作智能体：工作中 · 任务-001 · 已运行 4 分 12 秒。
- 审查智能体：空闲。

系统健康：

- 卡住任务：0。
- 执行失败：0。
- 验证未通过：0。
- 待人工处理：1。

说明文案：

> 智能体可以执行任务，接受结果与合入需要你确认。

## 占位页样例文案

### 工作流

这里后续用于沉淀可复用的成功任务流程。

V0.1.1 暂未开放工作流管理。

按钮：`返回首页`。

接受结果并选择 `保存为工作流` 后，工作流占位页展示静态示意卡：

- 名称：前端问题修复。
- 来源任务：修复登录页错误提示不显示，并补充测试。
- 包含步骤：阅读相关代码、定位问题根因、修复最小逻辑、补充测试、运行验证、生成审查包。
- 提示：V0.1.1 仅展示保存结果，不开放真实工作流管理。

### 智能体

这里后续用于查看计划智能体、工作智能体、审查智能体的档案与状态。

V0.1.1 暂未开放智能体管理。

按钮：`返回首页`。

### 设置

这里后续用于调整安全模式与工作台偏好。

V0.1.1 暂未开放设置管理。

按钮：`返回首页`。
