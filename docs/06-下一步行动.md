# 下一步行动

## 当前目标

基于 `docs/01-单一事实源-V0.1.1.md` 和 `docs/09-低保真原型任务书.md`，进入 `prototype/low-fi` 低保真可点击原型制作。

当前唯一事实源：

- `docs/01-单一事实源-V0.1.1.md`

配套执行文档：

- `docs/09-低保真原型任务书.md`
- `docs/03-状态机与跳转矩阵.md`
- `docs/04-样例数据与事件文案.md`
- `docs/05-低保真原型验收清单.md`

## 本轮要完成

- 创建 `prototype/low-fi` 低保真可点击原型。
- 覆盖六个核心页面和三个占位页。
- 走通“修复登录页错误提示不显示，并补充测试”的主流程。
- 使用 `docs/05-低保真原型验收清单.md` 做走查。
- 不继续扩写需求，不新增后端或真实智能体能力。

## 本轮之后不要继续做什么

- 不继续发散新功能。
- 不新增核心页面。
- 不恢复“运行 / 审查”一级导航。
- 不把工作流、智能体、设置展开为真实功能。
- 不写后端、数据库、模型调用、部署、权限、计费方案。

## 下一轮入口

下一轮应直接按 `docs/09-低保真原型任务书.md` 开始低保真原型：

1. 画整体外壳：顶栏、左侧导航、中间主工作区、右侧状态栏、底部事件流。
2. 画六个核心页面：首页、计划确认页、任务板、任务详情页、执行详情页、审查包页。
3. 画三个占位页：工作流、智能体、设置。
4. 按 `docs/04-样例数据与事件文案.md` 填充静态内容。
5. 按 `docs/03-状态机与跳转矩阵.md` 做可点击连线。
6. 按 `docs/05-低保真原型验收清单.md` 做走查。

## 走查样例

“修复登录页错误提示不显示，并补充测试。”

必须走通：

首页输入目标 → 计划确认页 → 任务详情页 → 执行详情页 → 审查包页 → 接受结果 → 任务板已完成列。

## 完成后

完成低保真原型后，先做主流程走查并整理问题，再决定是否进入高保真视觉。
