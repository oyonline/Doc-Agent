# 术语表

## 说明

- 范围: `docs/00` ~ `docs/08`。
- 口径: 若同一概念在多份文档中的表述不一致,以 `docs/01-V0.2-单一事实源.md` 指向的事实源为准。
- 处理原则: 主表只收当前正式术语; `docs/08` 等文档中仍残留的旧写法,统一放在文末单列说明。

## 智能体

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 产品智能体 | 规划阶段的主持智能体,负责组织协同澄清、整合澄清结论并并行产出 PRD。 | `docs/02 §4.1 产品智能体` | “产品智能体已展示澄清结论摘要,等待用户在闸门 1 决策。” |
| 架构师智能体 | 规划阶段的技术协同智能体,负责补技术问题、识别风险并并行产出可行性评估。 | `docs/02 §4.2 架构师智能体` | “架构师智能体在第 4 轮识别 R-001(XSS),随后进入可行性评估中。” |
| 任务拆解智能体 | 规划阶段的拆解智能体,负责把 PRD 和实现方向拆成有依赖关系的任务图。 | `docs/02 §4.3 任务拆解智能体` | “闸门 1 后,任务拆解智能体进入等待规划稳定中,随后开始拆解。” |
| 项目管理智能体 | 执行阶段的调度智能体,负责按任务依赖图分发任务并推进整体进度。 | `docs/02 §4.4 项目管理智能体` | “项目管理智能体已分发任务-2、任务-3 给后端智能体(并行)。” |
| 前端智能体 | 执行阶段的前端任务执行智能体,负责组件、页面、交互和样式类任务。 | `docs/02 §4.5 前端智能体` | “前端智能体同时执行任务-4、任务-5,并生成对应单任务审查包。” |
| 后端智能体 | 执行阶段的后端任务执行智能体,负责 API、数据库和业务逻辑类任务。 | `docs/02 §4.6 后端智能体` | “后端智能体已开始执行任务-1,随后推进任务-2、任务-3。” |
| 测试智能体 | 执行阶段的测试任务执行智能体,负责集成测试和端到端测试类任务。 | `docs/02 §4.7 测试智能体` | “V0.2.1 中测试智能体先执行任务-7,再执行任务-6。” |
| 审查智能体 | 审查阶段的复核智能体,负责单任务复审和需求级跨任务一致性检查。 | `docs/02 §4.8 审查智能体` | “任务-6 被接受后,审查智能体开始跨任务一致性检查。” |

## 状态

### 需求主状态

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 待澄清 | 需求刚提交或被打回澄清后,等待产品智能体启动或继续协同澄清的状态。 | `docs/03 §需求状态机 -> 主状态` | “需求-003 当前主状态为待澄清。” |
| 规划中 | 需求处于协同澄清或三产出并行阶段,尚未启动执行的状态。 | `docs/03 §需求状态机 -> 主状态` | “需求-001 在 14:10 进入规划中 · 产出子阶段。” |
| 执行中 | 任务图已就绪并进入多智能体执行的状态。 | `docs/03 §需求状态机 -> 主状态` | “用户点击 `启动执行` 后,需求-001 进入执行中。” |
| 待审查 | 全部任务执行完且需求级审查包已生成,等待人工最终决策的状态。 | `docs/03 §需求状态机 -> 主状态` | “15:42 时需求-001 进入待审查状态。” |
| 已完成 | 用户接受需求级审查包后的终态。 | `docs/03 §需求状态机 -> 主状态` | “用户点击 `接受需求结果` 后,需求进入已完成。” |
| 已放弃 | 用户在“放弃/拆分”异常分支中选择放弃或拆分后进入的异常终态。 | `docs/03 §需求状态机 -> 主状态` | “原需求被拆分后,主状态进入已放弃(拆分)。” |

### 需求异常标签

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 放弃/拆分 | 协同澄清达到 6 轮仍未对齐时,系统自动叠加在“待澄清”上的异常标签。 | `docs/03 §需求状态机 -> 异常标签` | “系统自动施加 `放弃/拆分` 后,用户可选继续尝试澄清、放弃或拆分。” |
| 部分拒绝 | 用户在需求级审查包选择“部分接受”后,叠加在需求上的异常标签。 | `docs/03 §需求状态机 -> 异常标签` | “任务-3、7 被重做时,需求状态会变为执行中 + 部分拒绝。” |
| 全量拒绝并反馈 | 用户在需求级审查包选择“拒绝并反馈”后,叠加在需求上的异常标签。 | `docs/03 §需求状态机 -> 异常标签` | “用户全量打回后,需求回到规划中或执行中 + 全量拒绝并反馈。” |

### 需求子阶段

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 澄清子阶段 | “规划中”内部的前半段,用于产品与架构师 4 轮协同澄清并展示澄清结论摘要。 | `docs/03 §需求状态机 -> 子阶段说明` | “需求详情页顶部进度指示在澄清子阶段高亮 `澄清`。” |
| 产出子阶段 | “规划中”内部的后半段,用于并行产出 PRD、可行性评估和任务图并进入闸门 2 决策。 | `docs/03 §需求状态机 -> 子阶段说明` | “闸门 1 后,需求进入产出子阶段,三智能体开始并行工作。” |

### 任务主状态

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 待处理 | 任务已进入任务池,但前置依赖尚未全部完成的状态。 | `docs/03 §任务状态机 -> 主状态` | “任务-4 在任务-7 完成前保持待处理。” |
| 执行中 | 对应智能体正在执行该任务的状态。 | `docs/03 §任务状态机 -> 主状态` | “任务-2、任务-3 在 T1+ 同时进入执行中。” |
| 验证中 | 代码改动完成后,任务内验证正在运行的状态。 | `docs/03 §任务状态机 -> 主状态` | “任务-3 在跑接口校验时会进入验证中。” |
| 待审查 | 任务内验证通过且单任务审查包已生成,等待调度推进或人工处理的状态。 | `docs/03 §任务状态机 -> 主状态` | “任务-1 在数据层闸门时显示为待审查。” |
| 已完成 | 任务被自动放行或被人工接受后的终态,但仍可在需求级被打回重做。 | `docs/03 §任务状态机 -> 主状态` | “任务-4、5 自动放行后进入已完成。” |

### 任务异常标签

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 已停止 | 用户在任务详情页点击 `停止` 后叠加在任务上的异常标签。 | `docs/03 §任务状态机 -> 异常标签` | “任务-2 被用户停止后显示为执行中 + 已停止。” |
| 执行失败 | 智能体在执行阶段失败时叠加在任务上的异常标签。 | `docs/03 §任务状态机 -> 异常标签` | “异常路径 1 中,任务-2 进入执行中 + 执行失败。” |
| 验证未通过 | 任务内验证至少一项失败时叠加在任务上的异常标签。 | `docs/03 §任务状态机 -> 异常标签` | “任务-3 验证 typo 后会显示验证中 + 验证未通过。” |
| 已拒绝 | 用户在单任务审查包选择 `拒绝并反馈` 后叠加在任务上的异常标签。 | `docs/03 §任务状态机 -> 异常标签` | “任务-1 被拒绝后回到待处理 + 已拒绝。” |
| 卡住 | 任务长时间无推进时叠加的静态提示标签。 | `docs/03 §任务状态机 -> 异常标签` | “任务-3 长时间无更新时会被标成执行中 + 卡住。” |
| 需要人工处理 | 任务处于人工介入节点、等待用户操作时叠加的提示标签。 | `docs/03 §任务状态机 -> 异常标签` | “任务-7 到达集成层闸门后会显示 `需要人工处理`。” |

### 运行主状态

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 未开始 | 运行记录已创建但尚未实际启动的状态。 | `docs/03 §运行状态机 -> 主状态` | “新建运行后,历史运行表会先显示未开始。” |
| 进行中 | 当前这次运行仍在执行的状态。 | `docs/03 §运行状态机 -> 主状态` | “任务-2 第 2 段会话所在运行仍处于进行中。” |
| 已完成 | 当前运行已成功结束的状态。 | `docs/03 §运行状态机 -> 主状态` | “任务-1 的首个运行在生成审查包后变为已完成。” |
| 已失败 | 当前运行因错误退出的状态。 | `docs/03 §运行状态机 -> 主状态` | “异常路径 1 会把当前运行推到已失败。” |
| 已停止 | 当前运行被用户主动停止后的状态。 | `docs/03 §运行状态机 -> 主状态` | “用户点击 `停止` 后,本次运行会标记为已停止。” |

### 智能体主状态

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 空闲 | 智能体当前没有承担对应阶段工作的主状态。 | `docs/03 §智能体状态机` | “任务-1 完成后,后端智能体从工作中回到空闲。” |
| 工作中 | 智能体当前正在承担任务,并可叠加细分状态文案的主状态。 | `docs/03 §智能体状态机` | “产品智能体在第 1 轮显示为工作中 · 主持澄清 · 第 1 轮。” |

## 闸门

> V0.2.2 不新增闸门,只增加 Context 管理深度。

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 冻结需求闸门(闸门 1) | 协同澄清完成后,由用户决定是否冻结需求并进入产出子阶段的规划闸门。 | `docs/00 §V0.2 范围 -> 闸门清单` | “14:10 用户点击 `冻结需求,开始规划`,触发闸门 1。” |
| 启动执行闸门(闸门 2) | 三产出完成后,由用户决定启动执行、重新澄清或小修补的规划闸门。 | `docs/00 §V0.2 范围 -> 闸门清单` | “14:15 用户点击 `启动执行`,通过闸门 2。” |
| 数据层闸门(闸门 3) | 任务-1 完成后暂停调度,等待用户接受单任务审查包的数据层人工介入点。 | `docs/00 §V0.2 范围 -> 闸门清单` | “T1 时刻任务-1 等待人工接受(数据层闸门)。” |
| 集成层闸门(V0.2.1 闸门 4) | V0.2.1 中任务-7 完成后新增的人工介入点,用于集成测试结果确认。 | `docs/00 §V0.2 范围 -> 闸门清单` | “T3 时刻任务-7 等待审查(闸门 4 · 集成层,V0.2.1)。” |
| 端到端闸门(V0.2.0 闸门 4 / V0.2.1 闸门 5) | 任务-6 完成后暂停调度,等待用户确认端到端测试结果的人工介入点。 | `docs/00 §V0.2 范围 -> 闸门清单` | “T5 时刻任务-6 等待审查(端到端闸门)。” |
| 需求级审查闸门(V0.2.0 闸门 5 / V0.2.1 闸门 6) | 需求级审查包生成后,由用户做最终接受、部分接受或拒绝并反馈的最终闸门。 | `docs/00 §V0.2 范围 -> 闸门清单` | “T5+ 时刻需求-001 等待最终审查(需求级审查闸门)。” |

## UI 元素

### 页面与全局组件

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 需求工作台 | V0.2 的主入口页,用于查看全部需求概览和当前待处理决策事项。 | `docs/08 §需求工作台` | “用户登录后默认落地到需求工作台。” |
| 需求详情页 | 需求级工作台,承载从协同澄清到需求级审查的全生命周期视图。 | `docs/08 §需求详情页` | “需求详情页会在单页内切换协同澄清、PRD、任务图和执行进度等视图。” |
| 任务板 | 按需求分组展示任务池与任务卡的全局任务总览页。 | `docs/08 §任务板` | “任务板会把需求-001 的 7 个任务放在同一分组下展示。” |
| 任务详情页 | 任务级下钻页,展示概览、上下文、验收标准和历史运行。 | `docs/08 §任务详情页` | “从任务图节点、任务卡或需求级审查包任务行都可以进入任务详情页。” |
| 执行详情页 | 运行级下钻页,展示一次运行的时间线、日志、改动和验证证据链。 | `docs/08 §执行详情页` | “点击任务详情页的历史运行行后,会跳转到执行详情页。” |
| 单任务审查包页 | 任务级最终决策页面,承载单任务回顾、diff、验证结果和底部决策区。 | `docs/08 §单任务审查包页` | “任务-1 到达数据层闸门后,用户会进入单任务审查包页做决定。” |
| 需求级审查包页 | 需求级最终决策页面,承载 PRD 对比交付、任务汇总和跨任务一致性问题清单。 | `docs/08 §需求级审查包页` | “所有任务完成后,需求级审查包页承载最终人工决策。” |
| 右侧状态栏 | 全局常驻、不可点击的展示组件,按三阶段分组展示 8 个智能体状态。 | `docs/08 §右侧状态栏` | “T-pre1 时,右侧状态栏会同时亮起三个规划阶段智能体。” |
| 待你决策事项 | 需求工作台右下角的事项列表,集中展示当前需要用户处理的澄清、任务审查或需求审查。 | `docs/08 §需求工作台 -> 页面区域` | “T1 时刻,待你决策事项会出现 `任务-1 等待审查`。” |

### 任务与执行呈现

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 任务卡 | 出现在任务板中的列表化任务单元,展示任务状态但不展示依赖关系。 | `docs/03 §任务表示形态说明` | “任务卡会直接展示任务编号、主状态、异常标签和验证结果。” |
| 任务图节点 | 出现在需求详情页任务图中的节点单元,用于表达任务在依赖图中的位置。 | `docs/03 §任务表示形态说明` | “点击任务图节点后,会下钻到对应任务详情页。” |
| 任务图 | 由任务图节点和连线组成的依赖图视图,用于表达任务拆解、依赖和并行关系。 | `docs/06 §任务图与依赖` | “V0.2.1 的任务图会在实线之外补充虚线契约依赖。” |
| 修改文件清单 | 审查包内列出本次任务改动文件的列表区,用于驱动同页差异预览刷新。 | `docs/08 §单任务审查包页 -> 页面区域` | “点击修改文件清单中的某一行,下方差异预览会同页切换。” |
| 同页静态差异预览 | 审查包页中与修改文件清单并列的 diff 展示区,不允许跳转出去查看。 | `docs/08 §单任务审查包页 -> 页面区域` | “单任务审查包要求同页静态差异预览,不能跳转到执行详情页看 diff。” |
| 上下文 Tab | V0.2.0 / V0.2.1 任务详情页的第 2 个 Tab,只展示初始上下文 8 字段。 | `docs/01 §“上下文 Tab” vs “决策与上下文 Tab”` | “在 V0.2.0 中,任务详情页仍显示 `上下文` Tab。” |
| 决策与上下文 Tab | V0.2.2 启用的任务详情页第 2 个 Tab,展示初始上下文、决策日志和会话分段记录。 | `docs/05 §UI 呈现规范 -> 任务详情页“决策与上下文”Tab` | “V0.2.2 中,任务-2 的决策与上下文 Tab 会显示 3 段会话记录。” |
| 会话分段记录 | 决策与上下文 Tab 中按段展开展示会话起止时间、归档原因、接续摘要和自检结果的区域。 | `docs/05 §UI 呈现规范 -> 块 3:会话分段记录` | “任务-4 第 1 段归档后,会话分段记录会新增一条记录。” |

### 审查与决策组件

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 单任务审查包 | 每个任务完成后生成的任务级审查资产,用于技术 review 和任务级决策。 | `docs/01 §“单任务审查包” vs “需求级审查包”` | “任务-2 单任务审查包已生成,但不会阻塞默认自动放行。” |
| 需求级审查包 | 全部任务完成后生成的需求级审查资产,用于产品验收和最终决策。 | `docs/01 §“单任务审查包” vs “需求级审查包”` | “15:42 时需求级审查包已生成,需求进入待审查。” |
| 任务汇总 | 需求级审查包中按任务展示状态、验证和风险的汇总表。 | `docs/06 §10.3 任务汇总` | “需求级审查包的任务汇总会同时展示 7 个任务的状态和风险。” |
| 跨任务一致性问题清单 | 需求级审查包中由审查智能体生成的跨任务问题列表,按严重、警告、提示分级。 | `docs/06 §10.4 跨任务一致性问题清单` | “问题 #1 会指出任务-2 与任务-3 的默认状态不一致。” |
| 决策选项 | 审查包底部固定决策区中的操作集合,用于承载接受、拒绝和复查相关动作。 | `docs/06 §10.7 决策选项` | “需求级审查包底部会固定展示四个决策选项。” |
| 更多操作 | 单任务审查包决策区中的次级操作入口,收纳 `重新执行` 和 `让审查智能体复查`。 | `docs/08 §单任务审查包页 -> 页面区域` | “单任务审查包不会把四个按钮平铺,而是把两项次级动作放进更多操作。” |

### 决策选项

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 接受结果 | 单任务审查包中的主决策动作,用于接受单任务结果并推进下游调度。 | `docs/03 §按钮触发矩阵` | “用户点击 `接受结果` 后,任务-1 从待审查进入已完成。” |
| 拒绝并反馈 | 审查包中的退回动作,要求用户填写反馈,把结果打回上一轮执行或规划。 | `docs/03 §按钮触发矩阵` | “拒绝任务-1 时,反馈会进入下一次运行上下文。” |
| 重新执行 | 单任务审查包中的重跑动作,会新建一次运行并让任务回到执行中。 | `docs/03 §按钮触发矩阵` | “点击 `重新执行` 后,任务待审查会回到执行中。” |
| 让审查智能体复查 | 审查包中的复查动作,保持当前主状态不变,但触发审查智能体重新工作。 | `docs/03 §按钮触发矩阵` | “用户不直接做结论时,可以先让审查智能体复查。” |
| 接受需求结果 | 需求级审查包中的最终接受动作,用于把需求从待审查推进到已完成。 | `docs/03 §按钮触发矩阵` | “点击 `接受需求结果` 后,需求会从待审查进入已完成。” |
| 部分接受 | 需求级审查包中的中间决策动作,允许保留部分任务结果并只重做被拒任务。 | `docs/03 §按钮触发矩阵` | “用户选择 `部分接受` 后,任务-3、7 会回到待处理。” |

## 工作流概念

### 澄清与规划

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 需求 | V0.2 中最上层的业务交付单元,由多个任务共同支撑并经过完整的澄清、执行和审查链路。 | `docs/00 §三层粒度` | “`新增用户标签功能` 是一个完整需求,不是单个任务。” |
| 协同澄清 | 产品智能体与架构师智能体共同面向用户进行的 4 轮需求对齐过程。 | `docs/00 §V0.2 范围` | “协同澄清固定演示 4 轮,第 4 轮由架构师主导。” |
| 澄清结论摘要 | 协同澄清结束后由产品智能体整合的 5 维度摘要,用于承接闸门 1 决策。 | `docs/06 §澄清结论摘要` | “用户会先看到澄清结论摘要,再决定是否冻结需求。” |
| 三产出 | 闸门 1 之后并行生成的 PRD、可行性评估和任务图三项规划产物。 | `docs/00 §V0.2 核心心智` | “冻结需求后,三产出会并行启动而不是串行交接。” |
| PRD | 由产品智能体产出的需求说明文档,包含背景、目标、范围、用户故事和需求级验收标准。 | `docs/06 §PRD` | “PRD 第 6 条记录了 XSS 双保险策略。” |
| 可行性评估 | 由架构师智能体产出的规划文档,用于沉淀风险、实现方向和关键技术决策。 | `docs/02 §4.2 架构师智能体` | “闸门 2 review 时,可行性评估会与 PRD 并列展示。” |
| 风险清单 | 可行性评估中的风险列表,统一采用高 / 中 / 低三档评级。 | `docs/02 §4.2 架构师智能体` | “R-001 在风险清单中直接定为中风险。” |
| 关键技术决策记录 | 可行性评估中记录关键技术取舍及其理由的条目集合。 | `docs/02 §4.2 架构师智能体` | “架构师会把 XSS 双保险落到关键技术决策记录里。” |

### 执行与调度

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 任务图 | 任务拆解智能体产出的依赖图,用于定义任务边界、前置关系和并行可能性。 | `docs/02 §4.3 任务拆解智能体` | “任务图会把任务-2、3 标成强并行,把任务-7 放在其后。” |
| 任务池 | 项目管理智能体接收并调度任务图后形成的可执行任务集合。 | `docs/02 §4.3 任务拆解智能体` | “闸门 2 启动执行后,任务图会被提交给项目管理智能体进入任务池调度。” |
| 调度规则 | 项目管理智能体必须遵守的 5 条硬规则,定义任务如何进入分发队列与何时暂停。 | `docs/01 §“调度规则”(docs/02)vs “调度核心原则”(docs/04)` | “任务-2、3 在任务-1 被接受后同时入队,体现了调度规则的真并行要求。” |
| 调度核心原则 | 在调度规则之上解释产品意图的 5 条原则,强调依赖驱动、自动放行和异常路径完整性。 | `docs/01 §“调度规则”(docs/02)vs “调度核心原则”(docs/04)` | “V0.2 强调自动放行是默认,人工介入是例外。” |
| 任务内验证 | 任务自带的单元测试、代码检查、类型检查或契约校验,不拆成独立测试任务。 | `docs/00 §V0.2 范围` | “任务-2 会在生成审查包前先完成 5/5 项任务内验证。” |
| 自动放行 | 非人工介入节点任务完成后,系统自动推进下游调度的默认机制。 | `docs/04 §调度核心原则` | “任务-4、5 完成后会自动放行任务-6。” |
| 人工介入节点 | 调度链路中少数必须暂停等待用户决策的节点集合。 | `docs/02 §4.4 项目管理智能体 -> 调度规则` | “任务-1 后、任务-7 后、任务-6 后都是人工介入节点。” |
| 模块独立并行 | 两个任务没有共享接口或文件时的并行类型。 | `docs/04 §并行的三种类型 -> 类型一` | “任务-4 与任务-5 属于模块独立并行。” |
| 契约先行的并行 | 任务之间存在接口契约关系,但契约先固定后可并行开发的并行类型。 | `docs/04 §并行的三种类型 -> 类型二` | “任务-2 与任务-4 代表契约先行的并行可能性。” |
| 完成时间错位的并行 | 下游任务可在上游达到某个里程碑后提前启动的并行类型。 | `docs/04 §并行的三种类型 -> 类型三` | “文档把任务-6 与任务-4 的真实工程关系当作完成时间错位的并行样例。” |
| 强数据依赖 | 因上游数据结构未完成,下游任务绝对不能启动的串行类型。 | `docs/04 §串行的三种类型 -> 类型一` | “任务-1 schema 完成前,任务-2 不能开始,这就是强数据依赖。” |
| 契约依赖 | 因契约本身尚未产出,下游任务必须等待上游完成的串行类型。 | `docs/04 §串行的三种类型 -> 类型二` | “任务-3 依赖任务-1 的 schema 定义,属于契约依赖。” |
| 汇聚型串行 | 下游任务必须等待所有前置任务都完成后才能开始的串行类型。 | `docs/04 §串行的三种类型 -> 类型三` | “任务-7 需要等任务-2、3 全部完成,属于汇聚型串行。” |
| 异常路径 | 失败、停止、拒绝、卡住等异常发生后,系统状态如何演进和恢复的时序剧本。 | `docs/01 §“异常标签” vs “异常路径”` | “用户在需求级选择部分接受,会触发异常路径 6。” |

### 审查、接受与 Context

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 决策日志 | 记录需求执行过程中关键决策及理由的时间序列条目集合,在 V0.2.2 进入任务详情页展示。 | `docs/01 §“决策日志”有两份,各有归属` | “任务-2 的游标分页决策会被写入需求级决策日志。” |
| 审查建议 | 审查包中给用户的推荐决策及替代方案说明。 | `docs/06 §10.6 审查建议` | “当前需求的审查建议是 `部分接受`。” |
| 跨任务一致性检查 | 审查智能体在需求级审查前执行的跨任务复核动作,用于找出任务间的契约和实现矛盾。 | `docs/02 §4.8 审查智能体` | “任务-6 被接受后,系统会启动跨任务一致性检查。” |
| 会话分段 | V0.2.2 中一次运行内部按段归档和接续的执行组织方式。 | `docs/03 §运行状态机 -> V0.2.2 扩展:会话分段` | “任务-2 在 V0.2.2 中会被拆成 3 段会话。” |
| 接续摘要 | 会话归档时产出的 5 字段摘要,用于把决策和进度传给下一段会话。 | `docs/05 §归档时必须保留的内容` | “任务-2 第 1 段结束后会生成一份 412 字的接续摘要。” |
| 自检结果 | 会话归档时附带的 3 项自检结论,用于确认摘要完整性和可接续性。 | `docs/05 §归档时必须保留的内容` | “每次归档后都要补上未完成事项检查、持久化检查和接续准备度。” |
| 接续会话 | 只读取任务卡、接续摘要和必要代码切片后继续执行的新会话。 | `docs/05 §接续会话的启动规则` | “任务-2 第 2 段只读取任务卡、第 1 段接续摘要和关键文件。” |

## 文档中出现但不纳入正式口径的旧写法

| 中文名 | 定义(一句话) | 出处文档章节 | 使用范例 |
|---|---|---|---|
| 澄清中 | 旧写法,曾被当作需求主状态使用,当前正式口径已收敛为“规划中 · 澄清子阶段”。 | `docs/08 §需求详情页` | “术语表不再把 `澄清中` 作为独立主状态收录。” |
| PRD 已定 | 旧写法,曾用于描述澄清后到执行前的过渡状态,当前正式口径并入“规划中”。 | `docs/08 §需求详情页` | “当前应表述为 `规划中 · 产出子阶段`,而不是 `PRD 已定`。” |
| 拆解中 | 旧写法,曾被用作需求级状态名,当前正式口径中它只保留为任务拆解智能体的细分文案。 | `docs/08 §需求详情页` | “当前主状态不用 `拆解中`,而用 `规划中 · 产出子阶段`。” |
| 已驳回 | 已废弃动线,不得再作为主状态、异常标签或页面动线出现。 | `docs/03 §严禁事项`; `docs/06 §严禁事项` | “文档已明确严禁让 `已驳回` 重新进入需求状态机。” |
| 确认进入 PRD | 已下线按钮名,当前正式按钮为 `冻结需求,开始规划`。 | `docs/07 §V0.2.0 阶段验收清单 -> 状态级` | “验收清单已注明原 `确认进入 PRD` 按钮已下线。” |
| 首页 | V0.1.1 时期的旧称呼,当前 V0.2 的正式主入口页名称为“需求工作台”。 | `docs/08 §需求工作台` | “V0.2 主入口页代替 V0.1.1 的首页。” |
